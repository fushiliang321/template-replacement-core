[package]
name = "template-replacement-core-wasm"
version = "1.6.0"
edition = "2024"
description = "模板替换核心wasm库"
license = "Apache 2.0"

[dependencies]
common = { path = "../common", features = ["general"] }

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]
