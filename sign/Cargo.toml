[package]
name = "template-replacement-sign-core-wasm"
version = "1.8.0"
edition = "2024"
description = "模板替换核心wasm库（签名版）"
license = "Apache 2.0"

[dependencies]
common = { path = "../common", features = ["param-sign"] }

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]